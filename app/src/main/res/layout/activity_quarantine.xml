<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="kashyap.in.yajurvedaproject.quarantine.QuarantineActivity">


    <FrameLayout
        android:id="@+id/flContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/bottomBar"
        android:layout_alignParentTop="true" />

    <LinearLayout
        android:id="@+id/bottomBar"
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:layout_alignParentBottom="true">

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/qmbottomNav"
            android:layout_width="match_parent"
            android:layout_height="65dp"
            android:layout_marginStart="4dp"
            android:layout_marginTop="0dp"
            android:layout_marginEnd="4dp"
            android:layout_marginBottom="4dp"
            android:background="@color/darkestPrimaryColor"
            android:paddingTop="3dp"
            android:paddingBottom="5dp"
            android:visibility="visible"
            app:itemIconTint="@color/color_selector"
            app:itemTextAppearanceActive="@style/BottomNavView.Active"
            app:itemTextAppearanceInactive="@style/BottomNavView"
            app:itemTextColor="@color/color_selector"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/qmmenu" />

    </LinearLayout>


</RelativeLayout>
